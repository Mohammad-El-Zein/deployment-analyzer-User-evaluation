apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  annotations:
    deployment-analyzer/depends-on: "auth-service"
spec:
  template:
    spec:
      containers:
        - name: api-gateway
          image: api-gateway:latest